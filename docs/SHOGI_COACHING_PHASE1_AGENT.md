# Phase 1 Agent Instruction

Execute the Phase 1 implementation Issue from latest `origin/main`.

Read the product SSOT and all `docs/SHOGI_COACHING_PHASE1*` documents first. Audit the existing Cloudflare queue/D1/PWA/worker contracts before selecting the concrete schema. Keep the PR narrowly scoped to Durable KIF First. Post all results to the Issue and only declare DONE when its acceptance criteria pass.

Recommended model: GPT-5.6 Sol / High.
