# 盤面座標・指し手表記統一 結果

- 実施日: 2026-09-10
- 対象リポジトリ: `perusonao/shogi-review`
- base SHA: `187a2895ae07f5c0edf0b40a6a52c494f2dd76ba`
- 対象範囲: 盤面座標表示、感想戦の実戦手／水匠5推奨手の表示表記

## 変更内容

- 通常盤面の上端に筋 `9 8 7 6 5 4 3 2 1`、右端に段 `一`〜`九`を追加した。
- 反転時は盤面の描画順と同時に筋を `1`〜`9`、段を `九`〜`一`へ反転し、画面上の各マスと座標を一致させた。
- 座標余白は上・右とも12pxとし、盤面領域の縮小を最小限にした。
- 実戦手と推奨手を、局面SFENとUSI moveを入力とする同一の日本語棋譜フォーマッタで生成するよう統一した。
- `▲/△`、筋、漢数字の段、駒名、`成`、`不成`、`打`、`同`、必要時の `右/左/直/上/引/寄`に対応した。
- `打`はUSIのdrop (`*`) の場合だけ付与する。
- 旧あかね戦のように各手USIがないデータは、前後のSFEN差分から実戦USIを復元する。これにより旧analysis内の不正確な実戦手文字列を表示の根拠にしない。
- 赤い実戦矢印と緑の推奨矢印も、表示と同じUSIを参照するよう統一した。

## 67手目検証

`20260910_taatoru_cat` の67手目直前局面を検証した。

- 実戦USI: `2h3g` → `▲3七銀`
- 水匠5推奨USI: `P*8c` → `▲8三歩打`
- 手番: 先手
- 8三: 空きマス
- 先手持駒: 歩1枚
- `P*8c`: drop move

したがって `▲8三歩打` の `打` は局面データと一致し、正しい。

## 回帰確認

- 先手通常移動: `▲7六歩`
- 後手通常移動: `△3四歩`
- 駒打ち: `▲4五歩打`
- 成: `▲2二角成`
- 不成: `▲2二角不成`
- 同: `▲同銀`
- 左右: `▲5五金右` / `▲5五金左`
- 対象3局の全262手について、駒名解決と `打` のUSI整合を確認した。
- 4局の全課題局面（6 / 5 / 3 / 3局面）で実戦手・推奨手を同じフォーマッタから生成できることを確認した。
- あかね戦30手目は前後局面から `P*2c` を復元し、`△2三歩打` と正しく表示した。
- 通常表示、反転表示、赤＝実戦、緑＝推奨、「次の課題局面へ」を実ブラウザで確認した。
- 棋譜一覧で既存4局がすべて「解析済み」として表示されることを確認した。
- 390×844 viewportで `scrollWidth = clientWidth = 390`、横overflowなしを確認した。
- ブラウザconsole errorなし。
- JavaScript構文確認成功。
- `git diff --check` 成功。

## 変更ファイル

- `index.html`
- `docs/reports/SHOGI_BOARD-COORD_MOVE-NOTATION_Result.md`

## PWA反映

ローカルPWAで変更を確認済み。公開PWAは本変更を`origin/main`へpush後、GitHub Pagesのデプロイ完了を確認する。
